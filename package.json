{
  "name": "tabot",
  "version": "0.1.0",
  "private": true,
  "description": "TABot: one static web page that reads math exit slips from photos and returns one spreadsheet.",
  "license": "MIT",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test 'test/unit/*.test.js'",
    "test:e2e": "playwright test --config test/e2e/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
